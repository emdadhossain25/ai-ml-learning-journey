# React Native Essentials for Android Developer

## My Background → React Native

**15 years native Android = 90% of what I need for React Native!**

## Key Mappings

| Android | React Native |
|---------|--------------|
| TextView | Text |
| Button | TouchableOpacity |
| RecyclerView | FlatList |
| Fragment | Screen Component |
| ViewModel | useState/useContext |
| LiveData | State updates |
| XML layouts | JSX |
| Kotlin | JavaScript/TypeScript |

## Quick Example

**Android:**
```kotlin
val counter = MutableLiveData<Int>(0)
button.setOnClickListener { 
  counter.value = counter.value!! + 1 
}
```

**React Native:**
```javascript
const [counter, setCounter] = useState(0);
<Button onPress={() => setCounter(counter + 1)} />
```

**Same logic, different syntax!**

## My Advantage

✅ Know mobile constraints  
✅ Understand performance  
✅ Camera, GPS, sensors experience  
✅ Published apps (Play Store)  
✅ Led mobile teams  

**React Native = New notation for concepts I already know!**
